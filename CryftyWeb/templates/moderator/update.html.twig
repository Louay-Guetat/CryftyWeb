<div class="row">
    <div class="column large-12 tab-12">
        {{ form_start(form) }}
        {{ form_row(form) }}
        <button type="submit">Envoyer</button>
        {{ form_end(form) }}
    </div>
</div>